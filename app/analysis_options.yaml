# VGV lint baseline + claudart Law 1 enforcement (enum-first, exhaustive switches).
include: package:very_good_analysis/analysis_options.yaml

analyzer:
  errors:
    missing_enum_constant_in_switch: error

linter:
  rules:
    public_member_api_docs: true
